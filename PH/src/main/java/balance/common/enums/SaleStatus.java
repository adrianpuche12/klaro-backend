package balance.common.enums;

/** Estado del ciclo de vida de una venta. */
public enum SaleStatus {
    /** Venta registrada, aún en turno abierto. */
    OPEN,
    /** Venta confirmada al cerrar el turno. */
    CONFIRMED
}
